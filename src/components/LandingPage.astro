---
import { useTranslations, getLocalizedPath } from '../i18n/utils';
import { ui } from '../i18n/translations';
import type { LandingPackage } from '../lib/landing-data';
import { isCheckoutEnabled } from '../lib/checkout-enabled';
import PricingSection from './PricingSection';
import madridAtmosphere480 from '../assets/madrid-atmosphere-480.webp';
import madridAtmosphere768 from '../assets/madrid-atmosphere-768.webp';
import madridAtmosphere1024 from '../assets/madrid-atmosphere-1024.webp';
import avatarAlejandro320 from '../assets/avatar-alejandro-team-320.webp';
import avatarAlejandro480 from '../assets/avatar-alejandro-team-480.webp';
import avatarAlejandro640 from '../assets/avatar-alejandro-team-640.webp';
import avatarAlin320 from '../assets/avatar-alin-team-320.webp';
import avatarAlin480 from '../assets/avatar-alin-team-480.webp';
import avatarAlin640 from '../assets/avatar-alin-team-640.webp';
import avatarIrene320 from '../assets/avatar-irene-team-320.webp';
import avatarIrene480 from '../assets/avatar-irene-team-480.webp';
import avatarIrene640 from '../assets/avatar-irene-team-640.webp';
import HeroSection from './landing/HeroSection.astro';
import ProblemsSection from './landing/ProblemsSection.astro';
import MethodSection from './landing/MethodSection.astro';
import LevelsSection from './landing/LevelsSection.astro';
import LeadCaptureForm from './LeadCaptureForm.tsx';
import {
  createCmsHomeTranslator,
  getDefaultCmsHomeContent,
  type CmsHomeContent,
} from '../lib/cms-home-content';

interface Props {
  lang: 'es' | 'en' | 'ru';
  packages: LandingPackage[];
  content?: CmsHomeContent;
}

const { lang, packages, content = getDefaultCmsHomeContent(Astro.props.lang) } = Astro.props;
const purchaseEnabled = isCheckoutEnabled(Astro);
const managedTranslate = createCmsHomeTranslator(useTranslations(lang), content);
const t = (key: string): string => String(managedTranslate(key));
const localizedUi = ui[lang];

// Data structure helper for complex arrays 
const problemsStatements = localizedUi.problems.statements;
const methodColumns = localizedUi.method.columns;
const progressLevels = localizedUi.progress.levels;
const teamMembers = localizedUi.team.members;
const faqItems = content.faq.items;
const leadCaptureTranslations = localizedUi.leadCapture;

const launchPricingPlans = {
    es: {
        individual_4x50_28d: {
            name: '4 clases individuales',
            description: 'Una plaza semanal concreta con profesor y horario identificados antes de pagar.',
            features: [],
        },
    },
    en: {
        individual_4x50_28d: {
            name: '4 individual classes',
            description: 'One specific weekly place with the teacher and time identified before payment.',
            features: [],
        },
    },
    ru: {
        individual_4x50_28d: {
            name: '4 индивидуальных занятия',
            description: 'Одно конкретное еженедельное место: преподаватель и время известны до оплаты.',
            features: [],
        },
    },
}[lang];

const pricingTranslations = {
    ...localizedUi.pricing,
    plans: launchPricingPlans,
};

const ireneMemberByLang = {
    es: {
        name: 'IRENE',
        role: 'Profesora',
        languages: ['ES', 'EN', 'CS', 'FR', 'LSE'],
        bio: 'Profesora de español con perfil internacional y comunicación accesible en español, inglés, checo, francés y lengua de signos española.',
    },
    en: {
        name: 'IRENE',
        role: 'Teacher',
        languages: ['ES', 'EN', 'CS', 'FR', 'LSE'],
        bio: 'Spanish teacher with an international profile and accessible communication in Spanish, English, Czech, French and Spanish Sign Language.',
    },
    ru: {
        name: 'IRENE',
        role: 'Преподаватель',
        languages: ['ES', 'EN', 'CS', 'FR', 'LSE'],
        bio: 'Преподаватель испанского с международным профилем: испанский, английский, чешский, французский и испанский жестовый язык.',
    },
}[lang];

const teamMembersWithIrene = [...teamMembers, ireneMemberByLang];
const teamImages = [
    {
        src: avatarAlejandro640.src,
        srcset: `${avatarAlejandro320.src} 320w, ${avatarAlejandro480.src} 480w, ${avatarAlejandro640.src} 640w`,
    },
    {
        src: avatarAlin640.src,
        srcset: `${avatarAlin320.src} 320w, ${avatarAlin480.src} 480w, ${avatarAlin640.src} 640w`,
    },
    {
        src: avatarIrene640.src,
        srcset: `${avatarIrene320.src} 320w, ${avatarIrene480.src} 480w, ${avatarIrene640.src} 640w`,
    },
];
const accessibilityCopy = {
    es: {
        skip: 'Saltar al contenido principal',
        primaryNav: 'Navegación principal',
        openMenu: 'Abrir menú',
        closeMenu: 'Cerrar menú',
        mobileMenu: 'Menú principal',
        heroAlt: 'Estudiante practicando español',
        atmosphereAlt: 'Vista urbana de Madrid',
    },
    en: {
        skip: 'Skip to main content',
        primaryNav: 'Primary navigation',
        openMenu: 'Open menu',
        closeMenu: 'Close menu',
        mobileMenu: 'Main menu',
        heroAlt: 'Student practising Spanish',
        atmosphereAlt: 'Urban view of Madrid',
    },
    ru: {
        skip: 'Перейти к основному содержанию',
        primaryNav: 'Основная навигация',
        openMenu: 'Открыть меню',
        closeMenu: 'Закрыть меню',
        mobileMenu: 'Главное меню',
        heroAlt: 'Ученик практикует испанский язык',
        atmosphereAlt: 'Городской вид Мадрида',
    },
}[lang];

const courseDetails = {
    es: {
        includesTitle: 'Qué incluye el curso',
        includesIntro: 'Un sistema para hablar mejor, revisar con criterio y mantener contacto real con España entre clases.',
        includes: [
            'Cuatro clases individuales en directo de 50 minutos por ciclo de 28 días.',
            'Documento vivo de clase con errores, vocabulario y deberes.',
            'Worksheets y materiales propios sobre vida, trabajo y cultura en España.',
            'Calendario, enlace de Meet y recordatorios desde el campus.',
            'Profesor y franja semanal identificados antes de pagar.',
        ],
        audienceTitle: 'A quién va dirigido',
        audienceIntro: 'Para adultos que no buscan clases baratas ni milagros, sino conversación adulta y trabajo serio.',
        audience: [
            'Profesionales y otros adultos que viven en España o se preparan para construir allí su vida.',
            'Estudiantes que se han quedado atascados entre A2, B1 y B2.',
            'Personas con interés cultural real, más allá de tópicos y frases de manual.',
            'Alumnos que quieren hablar de trabajo, ciudad, historia, libros, familia o burocracia sin quedarse fuera.',
        ],
    },
    en: {
        includesTitle: 'What the course includes',
        includesIntro: 'A system for better conversation, sharper review and real contact with Spain between classes.',
        includes: [
            'Four live individual 50-minute classes per 28-day cycle.',
            'A living class document with mistakes, vocabulary and homework.',
            'Original worksheets about life, work and culture in Spain.',
            'Calendar, Meet link and reminders from the campus.',
            'Teacher and weekly time identified before payment.',
        ],
        audienceTitle: 'Who it is for',
        audienceIntro: 'For adults who are not looking for cheap miracle classes, but serious work and adult conversation.',
        audience: [
            'Professionals and other adults living in Spain or preparing to build a life there.',
            'Students stuck between A2, B1 and B2.',
            'People with real cultural curiosity, beyond cliches and textbook phrases.',
            'Learners who want to discuss work, cities, history, books, family or bureaucracy without staying outside the conversation.',
        ],
    },
    ru: {
        includesTitle: 'Что входит в курс',
        includesIntro: 'Система для лучшего разговора, точного повторения и реального контакта с Испанией между занятиями.',
        includes: [
            'Четыре индивидуальных занятия по 50 минут за цикл из 28 дней.',
            'Живой документ урока с ошибками, лексикой и домашними заданиями.',
            'Авторские worksheets о жизни, работе и культуре в Испании.',
            'Calendar, ссылка Meet и напоминания из кампуса.',
            'Преподаватель и еженедельное время известны до оплаты.',
        ],
        audienceTitle: 'Для кого это',
        audienceIntro: 'Для взрослых, которые ищут не дешевые чудеса, а серьезную работу и взрослый разговор.',
        audience: [
            'Для профессионалов и других взрослых, которые живут в Испании или готовятся строить там жизнь.',
            'Для студентов, застрявших между A2, B1 и B2.',
            'Для людей с настоящим культурным интересом, не только клише и фразы из учебника.',
            'Для тех, кто хочет говорить о работе, городах, истории, книгах, семье и бюрократии.',
        ],
    },
}[lang];

const communityDetails = {
    es: {
        eyebrow: 'Una plaza concreta',
        title: 'Sabes con quién y cuándo antes de pagar',
        intro: 'No compras un banco anónimo de horas: eliges una plaza semanal real y ves sus condiciones antes de contratar.',
        points: [
            'La plaza identifica profesor, franja semanal, zona horaria y primera fecha.',
            'Cada ciclo incluye cuatro clases individuales de 50 minutos y se renueva cada 28 días.',
            'En cada ciclo puedes pedir el valor contractual de todas las clases no consumidas antes de la siguiente.',
        ],
    },
    en: {
        eyebrow: 'One specific place',
        title: 'Know who and when before paying',
        intro: 'You do not buy an anonymous bank of hours: choose a real weekly place and see its terms before purchasing.',
        points: [
            'The place identifies the teacher, weekly time, time zone and first date.',
            'Each cycle includes four individual 50-minute classes and renews every 28 days.',
            'In each cycle, before the next class, you may request the contractual value of every unconsumed class.',
        ],
    },
    ru: {
        eyebrow: 'Конкретное место',
        title: 'Преподаватель и время известны до оплаты',
        intro: 'Это не анонимный банк часов: вы выбираете реальное еженедельное место до покупки.',
        points: [
            'Место указывает преподавателя, еженедельное время, часовой пояс и первую дату.',
            'Каждый цикл включает четыре индивидуальных занятия по 50 минут и продлевается каждые 28 дней.',
            'В каждом цикле до следующего занятия можно запросить договорную стоимость всех неиспользованных занятий.',
        ],
    },
}[lang];

const purchaseNextSteps = purchaseEnabled
    ? {
    es: {
        eyebrow: 'Compra directa',
        title: 'Así funciona la contratación',
        intro: 'Elige una plaza real con profesor y horario, revisa las condiciones y continúa cuando el checkout esté habilitado.',
        steps: [
            {
                title: 'Elige una plaza',
                body: 'Ves el profesor, la franja semanal, la zona horaria y la primera fecha.',
            },
            {
                title: 'Revisa las condiciones',
                body: 'Antes de pagar ves cuatro clases de 50 minutos, 259 EUR, renovación cada 28 días y la garantía.',
            },
            {
                title: 'Compra en Stripe',
                body: 'La plaza queda retenida durante el checkout y solo se confirma al completar el pago.',
            },
        ],
    },
    en: {
        eyebrow: 'Direct purchase',
        title: 'How purchasing works',
        intro: 'Choose a real place with its teacher and weekly time, review the terms and continue when checkout is enabled.',
        steps: [
            {
                title: 'Choose a place',
                body: 'See the teacher, weekly time, time zone and first date.',
            },
            {
                title: 'Review the terms',
                body: 'Before paying, see four 50-minute classes, EUR 259, renewal every 28 days and the guarantee.',
            },
            {
                title: 'Purchase through Stripe',
                body: 'The place is held during checkout and confirmed only when payment completes.',
            },
        ],
    },
    ru: {
        eyebrow: 'Прямая покупка',
        title: 'Как работает покупка',
        intro: 'Выберите реальное место с преподавателем и еженедельным временем, проверьте условия и продолжите, когда checkout будет включён.',
        steps: [
            {
                title: 'Выберите место',
                body: 'Вы видите преподавателя, еженедельное время, часовой пояс и первую дату.',
            },
            {
                title: 'Проверьте условия',
                body: 'До оплаты вы видите 4 занятия по 50 минут, 259 EUR, продление каждые 28 дней и гарантию.',
            },
            {
                title: 'Купите через Stripe',
                body: 'Место удерживается на время checkout и подтверждается только после оплаты.',
            },
        ],
    },
}[lang]
    : {
    es: {
        eyebrow: 'Contacto',
        title: 'Así empezamos ahora',
        intro: 'El precio de 259 EUR está publicado. Escríbenos para comprobar disponibilidad y orientarte antes de abrir la compra directa.',
        steps: [
            {
                title: 'Cuéntanos qué necesitas',
                body: 'Nivel aproximado, objetivo y disponibilidad. Respondemos a la consulta.',
            },
            {
                title: 'Revisamos la oferta',
                body: 'Cuatro clases individuales de 50 minutos por 259 EUR, renovación cada 28 días y garantía proporcional.',
            },
            {
                title: 'Te orientamos',
                body: 'Te confirmamos plazas reales y siguientes pasos. La compra en Stripe se abrirá cuando habilitemos el pago.',
            },
        ],
    },
    en: {
        eyebrow: 'Contact',
        title: 'How we start right now',
        intro: 'The EUR 259 price is published. Write to us to check availability and get oriented before direct purchase opens.',
        steps: [
            {
                title: 'Tell us what you need',
                body: 'Approximate level, goal and availability. We reply to your enquiry.',
            },
            {
                title: 'We review the offer',
                body: 'Four individual 50-minute classes for EUR 259, renewal every 28 days and the proportional guarantee.',
            },
            {
                title: 'We guide you',
                body: 'We confirm real places and next steps. Stripe purchase opens when we enable payment.',
            },
        ],
    },
    ru: {
        eyebrow: 'Контакт',
        title: 'Как мы начинаем сейчас',
        intro: 'Цена 259 EUR опубликована. Напишите нам, чтобы уточнить наличие мест и сориентироваться до открытия прямой покупки.',
        steps: [
            {
                title: 'Расскажите, что нужно',
                body: 'Примерный уровень, цель и доступность. Мы ответим на запрос.',
            },
            {
                title: 'Разбираем предложение',
                body: 'Четыре индивидуальных занятия по 50 минут за 259 EUR, продление каждые 28 дней и пропорциональная гарантия.',
            },
            {
                title: 'Ориентируем дальше',
                body: 'Подтверждаем реальные места и следующие шаги. Покупка через Stripe откроется, когда включим оплату.',
            },
        ],
    },
}[lang];

const seoEntryRoutes = [
    {
        href: '/es/espanol-para-vivir-en-espana',
        label: 'Vivir en España',
        title: 'Español para vivir en España',
        description: 'Para adultos que quieren entrar en la vida del país con conversación, cultura y contacto real.',
    },
    {
        href: '/es/espanol-para-profesionales',
        label: 'Trabajo y ciudad',
        title: 'Español para profesionales',
        description: 'Para profesionales que necesitan hablar con confianza en reuniones, trabajo y vida diaria.',
    },
    {
        href: '/es/clases-de-conversacion-en-espanol',
        label: 'Conversación adaptada a ti',
        title: 'Clases de conversación en español',
        description: 'Para alumnos que entienden bastante, pero necesitan soltura, corrección y conversación real.',
    },
];

const s = {
    bg: 'bg-[#E0F7FA]',
    text: 'text-[#006064]',
    accent: 'bg-[#006064]',
    accentText: 'text-white',
    border: 'border-[#006064]',
    secondaryBg: 'bg-white',
    button: 'bg-[#006064] text-white hover:bg-[#004d40]',
};

// Language switcher link builder
const supportedLanguages: Props['lang'][] = ['es', 'en', 'ru'];
const getLangLink = (targetLang: Props['lang']) => `/${targetLang}`;

---

<div class={`min-h-screen overflow-x-clip ${s.bg} ${s.text} transition-colors duration-300 font-sans selection:bg-black selection:text-white`}>
    <a href="#main-content" class="sr-only focus:not-sr-only focus:fixed focus:left-4 focus:top-4 focus:z-[100] focus:bg-white focus:px-4 focus:py-3 focus:font-bold focus:text-[#006064] focus:outline focus:outline-4 focus:outline-[#006064]">
        {accessibilityCopy.skip}
    </a>
    
    {/* Navbar */}
    <nav data-testid="primary-navigation" aria-label={accessibilityCopy.primaryNav} class={`public-primary-navigation h-16 border-b-2 ${s.border} flex justify-between items-center px-4 md:px-8 sticky z-[70] ${s.bg}`}>
        <div class="font-display text-lg md:text-xl tracking-tighter mr-2 w-full md:w-auto whitespace-nowrap overflow-visible leading-tight py-1">{t('nav.brand')}</div>

        {/* Desktop Navigation Links (Hidden on Mobile) */}
        <div class="hidden md:flex gap-6 items-center">
            <a href="#metodo" class="font-mono text-xs uppercase tracking-wide hover:opacity-70 transition-opacity">{t('nav.method')}</a>
            <a href="#progreso" class="font-mono text-xs uppercase tracking-wide hover:opacity-70 transition-opacity">{t('nav.progress')}</a>
            <a href="#planes" class="font-mono text-xs uppercase tracking-wide hover:opacity-70 transition-opacity">{t('nav.plans')}</a>
            <a href="#equipo" class="font-mono text-xs uppercase tracking-wide hover:opacity-70 transition-opacity">{t('nav.team')}</a>
            <a href="#faq" class="font-mono text-xs uppercase tracking-wide hover:opacity-70 transition-opacity">{t('nav.faq')}</a>
            <a href={getLocalizedPath('/blog', lang)} class="font-mono text-xs uppercase tracking-wide hover:opacity-70 transition-opacity">{t('nav.blog')}</a>
        </div>

        {/* Desktop Actions (Hidden on Mobile) */}
        <div class="hidden md:flex flex-row items-center gap-4">
            {/* Language Switcher */}
            <div class="flex items-center gap-1 font-mono text-xs font-bold">
                {supportedLanguages.map((lng) => (
                    <a
                        href={getLangLink(lng)}
                        hreflang={lng}
                        lang={lng}
                        aria-current={lang === lng ? 'page' : undefined}
                        class={`px-2 py-1 border ${s.border} uppercase transition-colors inline-block no-underline ${lang === lng ? `${s.accent} ${s.accentText}` : 'text-[#006064] hover:bg-[#006064]/10'}`}
                    >
                        {lng}
                    </a>
                ))}
            </div>

            <a href={`/${lang}/login`} class={`px-4 py-1 text-xs font-bold uppercase border ${s.border} hover:opacity-50 transition-opacity`}>
                {t('nav.login')}
            </a>
        </div>

        {/* Hamburger Icon (Mobile Only) */}
        <button id="mobile-menu-btn" type="button" aria-label={accessibilityCopy.openMenu} data-open-label={accessibilityCopy.openMenu} data-close-label={accessibilityCopy.closeMenu} aria-expanded="false" aria-controls="mobile-overlay" class="md:hidden flex flex-col justify-center items-center w-10 h-10 border-2 border-[#006064] bg-[#E0F7FA] active:bg-[#006064] active:text-[#E0F7FA] transition-colors focus:outline-none focus-visible:ring-4 focus-visible:ring-[#006064] focus-visible:ring-offset-2 z-[80] relative group">
            <span class="block w-5 h-0.5 bg-current mb-1 transition-transform origin-center group-[.is-open]:rotate-45 group-[.is-open]:translate-y-[6px]"></span>
            <span class="block w-5 h-0.5 bg-current mb-1 transition-opacity group-[.is-open]:opacity-0"></span>
            <span class="block w-5 h-0.5 bg-current transition-transform origin-center group-[.is-open]:-rotate-45 group-[.is-open]:-translate-y-[6px]"></span>
        </button>
    </nav>

    {/* Mobile Overlay Menu */}
    <div id="mobile-overlay" role="navigation" aria-label={accessibilityCopy.mobileMenu} class="public-mobile-overlay fixed inset-x-0 bottom-0 bg-[#006064] z-[60] transform translate-x-full transition-transform duration-300 ease-in-out md:hidden flex flex-col" aria-hidden="true" hidden inert>
        {/* Menu Content */}
        <div class="flex flex-col flex-1 p-8 overflow-y-auto">
            <div class="flex flex-col gap-6 text-2xl font-display text-white mb-12">
                <a href="#metodo" class="mobile-link hover:text-[#E0F7FA]/70">{t('nav.method')}</a>
                <a href="#progreso" class="mobile-link hover:text-[#E0F7FA]/70">{t('nav.progress')}</a>
                <a href="#planes" class="mobile-link hover:text-[#E0F7FA]/70">{t('nav.plans')}</a>
                <a href="#equipo" class="mobile-link hover:text-[#E0F7FA]/70">{t('nav.team')}</a>
                <a href="#faq" class="mobile-link hover:text-[#E0F7FA]/70">{t('nav.faq')}</a>
                <a href={getLocalizedPath('/blog', lang)} class="mobile-link hover:text-[#E0F7FA]/70">{t('nav.blog')}</a>
            </div>

            <div class="mt-auto flex flex-col gap-6">
                {/* Mobile Language Switcher */}
                <div class="flex justify-center gap-2 font-mono text-sm font-bold w-full">
                    {supportedLanguages.map((lng) => (
                        <a
                            href={getLangLink(lng)}
                            hreflang={lng}
                            lang={lng}
                            aria-current={lang === lng ? 'page' : undefined}
                            class={`flex-1 py-3 text-center border-2 border-[#E0F7FA] uppercase transition-colors no-underline ${lang === lng ? 'bg-[#E0F7FA] text-[#006064]' : 'text-[#E0F7FA] hover:bg-[#E0F7FA]/10'}`}
                        >
                            {lng}
                        </a>
                    ))}
                </div>
                
                {/* Mobile Login Button */}
                <a href={`/${lang}/login`} class="w-full py-4 text-center font-bold uppercase tracking-widest border-2 border-[#E0F7FA] bg-[#E0F7FA] text-[#006064] active:bg-transparent active:text-[#E0F7FA] transition-colors">
                    {t('nav.login')}
                </a>
            </div>
        </div>
    </div>

    <main id="main-content">
    {/* Hero */}
    <HeroSection 
        headline1={t('hero.headline1')}
        headline2={t('hero.headline2')}
        headline3={t('hero.headline3')}
        manifesto={t('hero.manifesto')}
        subtitle={t('hero.subtitle')}
        ready={t('hero.ready')}
        ctaText={purchaseEnabled ? t('hero.cta') : t('common.ctaContact')}
        ctaHref={purchaseEnabled ? '#planes' : '#contacto'}
        imageAlt={accessibilityCopy.heroAlt}
    />

    {/* Ticker - Full Width */}
    <div class={`relative z-10 w-full border-b-2 ${s.border} overflow-hidden py-3 ${s.accent} ${s.accentText}`}>
        <div class="animate-marquee whitespace-nowrap font-mono font-bold text-sm md:text-base uppercase tracking-widest">
            {/* Simple repetition for ticker effect */}
            {Array(8).fill(t('ticker')).join('')}
        </div>
    </div>

    {/* El Problema */}
    <ProblemsSection 
        headline={t('problems.headline')}
        subtext={t('problems.subtext')}
        statements={problemsStatements}
    />

    {/* El Método */}
    <MethodSection 
        headline={t('method.headline')}
        manifesto={t('method.manifesto')}
        closing={t('method.closing')}
        subtitle={t('method.subtitle')}
        columns={methodColumns}
    />

    {/* Atmosphere Break */}
    <div class={`w-full h-64 md:h-96 overflow-hidden border-b-2 ${s.border} relative`}>
        <img
            src={madridAtmosphere768.src}
            srcset={`${madridAtmosphere480.src} 480w, ${madridAtmosphere768.src} 768w, ${madridAtmosphere1024.src} 1024w`}
            sizes="100vw"
            alt={accessibilityCopy.atmosphereAlt}
            width="1024"
            height="1024"
            class="w-full h-full object-cover grayscale"
            loading="lazy"
            decoding="async"
        />
        <div class="absolute inset-0 bg-[#006064]/20 mix-blend-multiply"></div>
    </div>

    {/* Tu Progreso / Niveles */}
    <LevelsSection 
        headline={t('progress.headline')}
        subheadline={t('progress.subheadline')}
        paragraph={t('progress.paragraph')}
        closing={t('progress.closing')}
        manifesto={t('progress.manifesto')}
        levels={progressLevels}
    />

    {/* Course Details */}
    <section id="incluye" class="bg-[#E0F7FA] py-24 px-6 border-b-2 border-[#006064]">
        <div class="max-w-6xl mx-auto grid grid-cols-1 gap-12 lg:grid-cols-2">
            <div>
                <h2 class="font-display text-4xl lg:text-5xl text-[#006064]">{courseDetails.includesTitle}</h2>
                <p class="mt-4 max-w-xl text-lg leading-8 text-[#006064]">{courseDetails.includesIntro}</p>
                <div class="mt-8 space-y-3">
                    {courseDetails.includes.map((item) => (
                        <div class="border-2 border-[#006064] bg-white p-4 shadow-[4px_4px_0px_0px_#006064]">
                            <p class="font-sans text-base font-bold leading-7 text-[#006064]">{item}</p>
                        </div>
                    ))}
                </div>
            </div>
            <div>
                <h2 class="font-display text-4xl lg:text-5xl text-[#006064]">{courseDetails.audienceTitle}</h2>
                <p class="mt-4 max-w-xl text-lg leading-8 text-[#006064]">{courseDetails.audienceIntro}</p>
                <div class="mt-8 space-y-3">
                    {courseDetails.audience.map((item) => (
                        <div class="border-2 border-[#006064] bg-white p-4 shadow-[4px_4px_0px_0px_#006064]">
                            <p class="font-sans text-base font-bold leading-7 text-[#006064]">{item}</p>
                        </div>
                    ))}
                </div>
            </div>
        </div>
    </section>

    {/* Honest community */}
    <section id="comunidad" class="bg-white py-24 px-6 border-b-2 border-[#006064]">
        <div class="max-w-6xl mx-auto grid grid-cols-1 gap-12 lg:grid-cols-5">
            <div class="lg:col-span-2">
                <p class="font-mono text-xs uppercase tracking-widest text-[#006064] mb-4">{communityDetails.eyebrow}</p>
                <h2 class="font-display text-4xl lg:text-5xl text-[#006064]">{communityDetails.title}</h2>
                <p class="mt-4 text-lg leading-8 text-[#006064]/80 font-bold">{communityDetails.intro}</p>
            </div>
            <div class="lg:col-span-3 grid grid-cols-1 gap-4">
                {communityDetails.points.map((point) => (
                    <p class="border-2 border-[#006064] bg-[#E0F7FA] p-5 text-base font-bold leading-7 text-[#006064] shadow-[4px_4px_0px_0px_#006064]">
                        {point}
                    </p>
                ))}
            </div>
        </div>
    </section>

    {lang === 'es' && (
        <section id="rutas" class="bg-white py-24 px-6 border-b-2 border-[#006064]">
            <div class="max-w-6xl mx-auto grid grid-cols-1 gap-12 lg:grid-cols-3">
                <div>
                    <p class="font-mono text-xs uppercase tracking-widest text-[#006064] mb-4">Rutas de entrada</p>
                    <h2 class="font-display text-4xl lg:text-5xl text-[#006064]">Encuentra tu punto de partida</h2>
                    <p class="mt-4 max-w-xl text-lg leading-8 text-[#006064]">
                        Si ya sabes por qué necesitas español, entra por la página que mejor encaja contigo.
                    </p>
                </div>
                <div class="lg:col-span-2 grid grid-cols-1 md:grid-cols-2 gap-4">
                    {seoEntryRoutes.map((route) => (
                        <a href={route.href} class="group border-2 border-[#006064] bg-[#E0F7FA] p-5 shadow-[4px_4px_0px_0px_#006064] transition-transform hover:-translate-y-1 focus:outline-none focus:ring-4 focus:ring-[#006064] focus:ring-offset-2">
                            <p class="font-mono text-xs uppercase tracking-widest text-[#006064]">{route.label}</p>
                            <h3 class="mt-3 font-display text-2xl text-[#006064]">{route.title}</h3>
                            <p class="mt-3 font-sans text-base font-bold leading-7 text-[#006064]">{route.description}</p>
                            <span class="mt-5 inline-block font-mono text-xs uppercase font-bold border-b-2 border-[#006064] group-hover:opacity-70">
                                Ver ruta
                            </span>
                        </a>
                    ))}
                </div>
            </div>
        </section>
    )}

    {/* Pricing */}
    <PricingSection 
        client:load
        packages={packages}
        lang={lang}
        purchaseEnabled={purchaseEnabled}
        translations={pricingTranslations}
    />

    {/* Quiénes Somos (Team) */}
    <section id="equipo" class="bg-white py-24 px-6 border-b-2 border-[#006064]">
        <div class="max-w-6xl mx-auto">
            <div class="text-center mb-16">
                <h2 class="font-display text-4xl lg:text-5xl text-[#006064] mb-8 hyphens-auto break-words">{t('team.headline')}</h2>

                <div class="max-w-md mx-auto mb-8">
                    <p class="text-[#006064] font-mono text-sm md:text-base uppercase tracking-wide mb-4">{t('team.manifesto')}</p>
                    <p class="font-sans text-lg md:text-xl text-[#006064] font-bold leading-tight">{t('team.closing')}</p>
                </div>

                <p class="font-sans text-xl text-[#006064] max-w-2xl mx-auto">{t('team.subtitle')}</p>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-3 gap-10">
                {teamMembersWithIrene.map((member, index) => {
                    const memberImage = teamImages[index];
                    const fallbackInitials = member.name.slice(0, 2).toUpperCase();

                    return (
                    <div class="flex flex-col">
                        <div class="aspect-square bg-gray-200 rounded-lg mb-4 w-full overflow-hidden border-2 border-[#006064]">
                            {memberImage ? (
                                <img
                                    src={memberImage.src}
                                    srcset={memberImage.srcset}
                                    alt={member.name}
                                    sizes="(min-width: 768px) 33vw, 100vw"
                                    width="640"
                                    height="640"
                                    class="w-full h-full object-cover grayscale hover:grayscale-0 transition-all duration-500"
                                    loading="lazy"
                                    decoding="async"
                                />
                            ) : (
                                <div
                                    class="w-full h-full bg-[#E0F7FA] text-[#006064] flex items-center justify-center"
                                    aria-hidden="true"
                                >
                                    <span class="font-display text-6xl">{fallbackInitials}</span>
                                </div>
                            )}
                        </div>
                        <h3 class="font-display text-2xl text-[#006064] mb-1">{member.name}</h3>
                        <p class="font-sans text-sm uppercase tracking-wide text-[#006064] mb-4">{member.role}</p>
                        <div class="flex flex-wrap gap-2 mb-4">
                            {member.languages.map((langString: string) => (
                                <span class="bg-[#006064]/10 text-[#006064] text-xs px-2 py-1 rounded font-bold">
                                    {langString}
                                </span>
                            ))}
                        </div>
                        <p class="font-sans text-base text-[#006064]">
                            {member.bio}
                        </p>
                    </div>
                    );
                })}
            </div>
        </div>
    </section>

    {/* FAQ */}
    <section id="faq" class="bg-[#E0F7FA] py-24 px-6 border-b-2 border-[#006064]">
        <div class="max-w-4xl mx-auto">
            <div class="text-center mb-16">
                <h2 class="font-display text-4xl lg:text-5xl text-[#006064]">{t('faq.headline')}</h2>
            </div>

            <div class="space-y-4">
                {faqItems.map((item, index) => (
                    <div class="border-b border-[#006064]/20">
                        <button
                            id={`faq-button-${index}`}
                            type="button"
                            data-faq-button={index}
                            class="w-full flex justify-between items-center py-6 text-left focus:outline-none focus-visible:ring-4 focus-visible:ring-[#006064] focus-visible:ring-offset-2 group"
                            aria-expanded="false"
                            aria-controls={`faq-content-${index}`}
                        >
                            <span class="font-sans font-semibold text-lg text-[#006064] pr-8">{item.question}</span>
                            <span class="faq-icon text-[#006064] text-2xl font-bold transition-transform duration-300" aria-hidden="true">
                                +
                            </span>
                        </button>
                        <div
                            id={`faq-content-${index}`}
                            class="faq-content overflow-hidden transition-all duration-300 max-h-0 opacity-0"
                            role="region"
                            aria-labelledby={`faq-button-${index}`}
                            aria-hidden="true"
                            hidden
                        >
                            <p class="font-sans text-base text-[#006064] leading-relaxed pb-6">
                                {item.answer}
                            </p>
                        </div>
                    </div>
                ))}
            </div>
        </div>
    </section>

    {/* Lead Capture Section */}
    <section id="contacto" class="py-24 px-6 border-t-2 border-[#006064] bg-[#E0F7FA]">
        <div class="max-w-6xl mx-auto grid grid-cols-1 gap-10 lg:grid-cols-[1fr_28rem] lg:items-start">
            <div>
                <p class="font-mono text-xs uppercase tracking-widest text-[#006064] mb-4">{purchaseNextSteps.eyebrow}</p>
                <h2 class="font-display text-4xl lg:text-5xl text-[#006064]">{purchaseNextSteps.title}</h2>
                <p class="mt-4 max-w-xl text-lg leading-8 text-[#006064]">{purchaseNextSteps.intro}</p>
                <div class="mt-8 grid grid-cols-1 gap-4">
                    {purchaseNextSteps.steps.map((step, index) => (
                        <div class="border-2 border-[#006064] bg-white p-5 shadow-[4px_4px_0px_0px_#006064]">
                            <p class="font-mono text-xs uppercase tracking-widest text-[#006064]">0{index + 1}</p>
                            <h3 class="mt-2 font-display text-2xl text-[#006064]">{step.title}</h3>
                            <p class="mt-2 font-sans text-base font-bold leading-7 text-[#006064]">{step.body}</p>
                        </div>
                    ))}
                </div>
            </div>
            <div>
                <LeadCaptureForm
                client:load
                lang={lang}
                translations={leadCaptureTranslations}
            />
            </div>
        </div>
    </section>
    </main>

    {/* Footer */}
    <footer class={`${s.accent} ${s.accentText} py-24 px-6 border-t-2 ${s.border}`}>
        <div class="max-w-7xl mx-auto grid grid-cols-1 md:grid-cols-2 gap-12">
            <div>
                <h2 class="font-display text-[12vw] md:text-[8vw] leading-[0.8] tracking-tighter">{t('footer.cta1')}</h2>
                <h2 class="font-display text-[12vw] md:text-[8vw] leading-[0.8] tracking-tighter opacity-50">{t('footer.cta2')}</h2>
            </div>
            <div class="flex flex-col justify-end items-start md:items-end">
                <div class="font-mono text-sm uppercase tracking-widest mb-8 text-left md:text-right">
                    {t('footer.address')}<br />{t('footer.city')}<br />{t('footer.email')}
                </div>
                <div class="flex gap-4 mb-4">
                    <a href={`/${lang}/legal`} class="font-mono text-xs uppercase text-[#006064] hover:underline transition-colors">
                        {t('legal.title')}
                    </a>
                </div>
                <p class="font-sans text-xs text-[#006064]">&copy; {new Date().getFullYear()} {t('footer.copyright')}</p>
            </div>
        </div>
    </footer>
</div>

<script>
    const faqCloseDelayMs = 300;
    const faqButtons = document.querySelectorAll<HTMLButtonElement>('[data-faq-button]');

    const setFaqOpen = (button: HTMLButtonElement, isOpen: boolean) => {
        const index = button.getAttribute('data-faq-button');
        const content = document.getElementById(`faq-content-${index}`);
        const icon = button.querySelector<HTMLElement>('.faq-icon');

        if (!content) return;

        button.setAttribute('aria-expanded', String(isOpen));
        content.setAttribute('aria-hidden', String(!isOpen));

        if (isOpen) {
            content.hidden = false;
            requestAnimationFrame(() => {
                content.classList.remove('max-h-0', 'opacity-0');
                content.classList.add('max-h-[64rem]', 'opacity-100');
            });
            icon?.classList.add('rotate-45');
            return;
        }

        content.classList.add('max-h-0', 'opacity-0');
        content.classList.remove('max-h-[64rem]', 'opacity-100');
        icon?.classList.remove('rotate-45');

        window.setTimeout(() => {
            if (button.getAttribute('aria-expanded') !== 'true') {
                content.hidden = true;
            }
        }, faqCloseDelayMs);
    };

    faqButtons.forEach(button => {
        button.addEventListener('click', () => {
            const isExpanded = button.getAttribute('aria-expanded') === 'true';

            faqButtons.forEach(otherButton => {
                if (otherButton !== button) setFaqOpen(otherButton, false);
            });

            setFaqOpen(button, !isExpanded);
        });
    });

    // Mobile Menu Logic
    const menuBtn = document.getElementById('mobile-menu-btn');
    const overlay = document.getElementById('mobile-overlay');
    const mobileLinks = document.querySelectorAll('.mobile-link');
    const firstMobileLink = overlay?.querySelector<HTMLElement>('.mobile-link');
    const menuCloseDelayMs = 300;
    let menuCloseTimer = 0;
    let isMenuOpen = false;

    const setMenuOpen = (nextOpen: boolean) => {
        isMenuOpen = nextOpen;
        menuBtn?.setAttribute('aria-expanded', String(isMenuOpen));
        if (menuBtn) {
            const nextLabel = isMenuOpen ? menuBtn.dataset.closeLabel : menuBtn.dataset.openLabel;
            if (nextLabel) menuBtn.setAttribute('aria-label', nextLabel);
        }

        if (isMenuOpen) {
            window.clearTimeout(menuCloseTimer);
            if (overlay) {
                overlay.hidden = false;
                overlay.removeAttribute('inert');
                overlay.setAttribute('aria-hidden', 'false');
                requestAnimationFrame(() => {
                    overlay.classList.remove('translate-x-full');
                    overlay.classList.add('translate-x-0');
                    firstMobileLink?.focus();
                });
            }
            menuBtn?.classList.add('is-open', 'bg-transparent', 'text-white', 'border-transparent');
            menuBtn?.classList.remove('bg-[#E0F7FA]', 'text-[#006064]', 'border-[#006064]');
            document.body.classList.add('overflow-hidden');
        } else {
            if (overlay) {
                overlay.classList.add('translate-x-full');
                overlay.classList.remove('translate-x-0');
                overlay.setAttribute('aria-hidden', 'true');
                overlay.setAttribute('inert', '');
                menuCloseTimer = window.setTimeout(() => {
                    if (!isMenuOpen) overlay.hidden = true;
                }, menuCloseDelayMs);
            }
            menuBtn?.classList.remove('is-open', 'bg-transparent', 'text-white', 'border-transparent');
            menuBtn?.classList.add('bg-[#E0F7FA]', 'text-[#006064]', 'border-[#006064]');
            document.body.classList.remove('overflow-hidden');
        }
    };

    const toggleMenu = () => setMenuOpen(!isMenuOpen);

    menuBtn?.addEventListener('click', toggleMenu);

    document.addEventListener('keydown', (event) => {
        if (event.key === 'Escape' && isMenuOpen) {
            setMenuOpen(false);
            menuBtn?.focus();
        }
    });

    mobileLinks.forEach(link => {
        link.addEventListener('click', () => {
            if (isMenuOpen) setMenuOpen(false);
        });
    });
</script>
