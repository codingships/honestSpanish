---
interface Props {
  headline: string;
  subtext: string;
  statements: readonly string[];
}

const { headline, subtext, statements } = Astro.props;
---
<section
    class="problems-section bg-white py-24 px-6 border-b-2 border-[#006064]"
>
    <div class="max-w-6xl mx-auto grid grid-cols-1 lg:grid-cols-5 gap-12">
        {/* Left Column */}
        <div class="lg:col-span-2">
            <h2 class="font-brand-display text-4xl lg:text-5xl text-[#006064] mb-4 leading-[1.3]">{headline}</h2>
            <p class="font-sans text-xl text-[#006064]/70 font-medium">{subtext}</p>
        </div>

        {/* Right Column (Pain Points) */}
        <div class="lg:col-span-3 space-y-8">
            {statements.map((statement, index) => (
                <div class="relative pl-2">
                    <span aria-hidden="true" class="absolute -top-6 -left-4 font-brand-display text-8xl text-[#006064]/10 select-none pointer-events-none">
                        0{index + 1}
                    </span>
                    <p class="relative z-10 font-sans text-lg md:text-xl text-[#006064] font-medium leading-relaxed">
                        {statement}
                    </p>
                </div>
            ))}
        </div>
    </div>
</section>

<style>
    .problems-section {
        background-image: url('../../assets/noise-texture-400.jpg');
        background-size: 200px;
    }
</style>
