---
export const prerender = false;
// English is the initial acquisition language. Keep the root as a permanent,
// canonical entry point rather than serving a second copy of the home page.
return Astro.redirect('/en', 301);
---
