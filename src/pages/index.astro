---
export const prerender = true;
import { getLocalizedPath } from '../i18n/utils';

// Redirección 301 Permanente (Vital para no perder Link Juice de espanolhonesto.com -> /es)
return Astro.redirect('/es/', 301);
---
